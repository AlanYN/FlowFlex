---
phase: 03-frontend-ux-data
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/flowFlex-backend/Application.Contracts/Dtos/OW/Questionnaire/QuestionnaireFileUploadResponseDto.cs
  - packages/flowFlex-backend/WebApi/Controllers/OW/QuestionnaireController.cs
  - packages/flowFlex-common/src/app/views/onboard/onboardingList/components/dynamicForm.vue
autonomous: true
requirements:
  - DATA-01

must_haves:
  truths:
    - "After uploading a file in a questionnaire, the file entry shows 'Uploaded by {name}, MM/DD/YYYY HH:mm:ss' to the right of the filename"
    - "Historical file answers without uploadedBy/uploadDate render without error — the metadata line is simply omitted"
    - "The backend file upload response includes an UploadedBy field containing the uploader's display name"
  artifacts:
    - path: "packages/flowFlex-backend/Application.Contracts/Dtos/OW/Questionnaire/QuestionnaireFileUploadResponseDto.cs"
      provides: "UploadedBy string property on upload response DTO"
      contains: "UploadedBy"
    - path: "packages/flowFlex-backend/WebApi/Controllers/OW/QuestionnaireController.cs"
      provides: "UploadedBy assigned from _userContext.UserName in UploadQuestionFileAsync"
      contains: "UploadedBy"
    - path: "packages/flowFlex-common/src/app/views/onboard/onboardingList/components/dynamicForm.vue"
      provides: "Metadata display block rendered conditionally for file answers"
      contains: "uploadedBy"
  key_links:
    - from: "QuestionnaireController.UploadQuestionFileAsync"
      to: "QuestionnaireFileUploadResponseDto.UploadedBy"
      via: "_userContext.UserName assignment"
      pattern: "UploadedBy"
    - from: "dynamicForm.vue upload response handler"
      to: "formData file object"
      via: "uploadedBy + uploadDate injection after API response"
      pattern: "uploadedBy"
    - from: "dynamicForm.vue file render block"
      to: "timeZoneConvert"
      via: "v-if guard on file.uploadedBy || file.uploadDate"
      pattern: "uploadedBy.*uploadDate|uploadDate.*uploadedBy"
---

<objective>
Full-stack change: add uploader name and upload timestamp to the file upload response, store both fields in the answer JSONB, and display them in the questionnaire form.

Purpose: Users need to know who uploaded a file and when, without navigating away from the questionnaire. This closes DATA-01 per D-07, D-08, D-09.

Output:
- QuestionnaireFileUploadResponseDto.cs — new UploadedBy string property (per D-07)
- QuestionnaireController.cs — populate UploadedBy from _userContext.UserName in UploadQuestionFileAsync (per D-07)
- dynamicForm.vue — locate the upload API call, inject uploadedBy + uploadDate into the stored file object, add conditional metadata display in the file render block (per D-08, D-09)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DATA-01 Backend — Add UploadedBy to file upload response DTO and controller (per D-07)</name>
  <files>
    packages/flowFlex-backend/Application.Contracts/Dtos/OW/Questionnaire/QuestionnaireFileUploadResponseDto.cs
    packages/flowFlex-backend/WebApi/Controllers/OW/QuestionnaireController.cs
  </files>
  <read_first>
    packages/flowFlex-backend/Application.Contracts/Dtos/OW/Questionnaire/QuestionnaireFileUploadResponseDto.cs — Read the full file. Note all existing properties (Id, Success, AccessUrl, OriginalFileName, FileName, FilePath, FileSize, ContentType, Category, FileHash, UploadTime, Gateway, FullAccessUrl). Note the namespace.
    packages/flowFlex-backend/WebApi/Controllers/OW/QuestionnaireController.cs — Read the UploadQuestionFileAsync action method. Identify: how the response DTO is constructed (object initializer or mapped from service result), where UploadTime is assigned, and confirm that _userContext.UserName is accessible (it is used elsewhere in the same controller).
  </read_first>
  <action>
    Change 1 — QuestionnaireFileUploadResponseDto.cs:

    Add one new public property after UploadTime:
    `/// <summary>Display name of the user who uploaded the file</summary>`
    `public string UploadedBy { get; set; }`

    Place it adjacent to UploadTime for logical grouping. No other changes to the DTO.

    Change 2 — QuestionnaireController.cs, UploadQuestionFileAsync method:

    In the response DTO construction block (the object initializer that sets UploadTime and other fields), add:
    `UploadedBy = _userContext.UserName,`

    If the response DTO is built via AutoMapper from a service result object rather than an object initializer, set the property directly on the mapped result before returning: `result.UploadedBy = _userContext.UserName;`

    Do not change the method signature, route, authorization attributes, or any other controller method.
  </action>
  <verify>
    <automated>cd packages/flowFlex-backend && dotnet build --no-restore -warnaserror 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - QuestionnaireFileUploadResponseDto.cs contains `public string UploadedBy { get; set; }` — grep: `grep -n "UploadedBy" packages/flowFlex-backend/Application.Contracts/Dtos/OW/Questionnaire/QuestionnaireFileUploadResponseDto.cs` returns a match
    - QuestionnaireController.cs assigns UploadedBy in UploadQuestionFileAsync — grep: `grep -n "UploadedBy" packages/flowFlex-backend/WebApi/Controllers/OW/QuestionnaireController.cs` returns a match
    - `dotnet build` succeeds with no errors
    - The upload endpoint response JSON includes an `uploadedBy` field (camelCase via JSON serializer) containing the uploader's display name
  </acceptance_criteria>
  <done>Backend upload response includes UploadedBy; dotnet build passes with no errors</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DATA-01 Frontend — Inject upload metadata into formData and display it in file render block (per D-07, D-08, D-09)</name>
  <files>
    packages/flowFlex-common/src/app/views/onboard/onboardingList/components/dynamicForm.vue
  </files>
  <read_first>
    packages/flowFlex-common/src/app/views/onboard/onboardingList/components/dynamicForm.vue — Read the entire file in sections:
      1. The imports block — confirm whether timeZoneConvert and projectTenMinutesSsecondsDate are already imported. If not, note the import paths from RESEARCH.md: `import { timeZoneConvert } from '@/hooks/time'` and `import { projectTenMinutesSsecondsDate } from '@/settings/projectSetting'`.
      2. Search for `upload-file` or `uploadFile` or `handleUpload` or `el-upload` to find the manual upload trigger and the point where the upload API response is processed. Note the exact function name and the variable that receives the response.
      3. The file question render block in the template — find where existing file answers are displayed (look for file.name or file.url rendering, the list of uploaded files under a file-type question). Note the surrounding element structure.
      4. Lines 1163–1183 — the validateForm short_answer_grid block (read-only context, do not change in this task).
    packages/flowFlex-common/src/app/apis/ow/ — Grep for "upload-file" or "uploadFile" to confirm the API function name and its import path in dynamicForm.vue.
  </read_first>
  <action>
    Step A — Ensure imports. If `timeZoneConvert` is not imported, add `import { timeZoneConvert } from '@/hooks/time';`. If `projectTenMinutesSsecondsDate` is not imported, add `import { projectTenMinutesSsecondsDate } from '@/settings/projectSetting';`. Do not duplicate existing imports.

    Step B — Inject metadata at upload response point. Locate the function that calls the file upload API and processes the response (the manual upload trigger identified during read_first). After the successful response is received, enrich the file object that gets stored in formData with two additional fields:
    - `uploadedBy`: set to `response.uploadedBy` (the new field from the backend DTO, camelCase in JSON)
    - `uploadDate`: set to `response.uploadTime` (the existing UTC ISO string already on the DTO)

    The enrichment should happen before or at the point the file reference is pushed/assigned into formData. If the response object is spread into a new object, add these two fields in the spread. If the existing file object in formData is mutated, assign the two properties directly. Either approach is acceptable — match the existing pattern in the function.

    Step B note: `response.uploadTime` is the existing field (DateTime UTC from backend, already a string in JSON). `response.uploadedBy` is the new field added in Task 1. Both come from the same upload response object.

    Step C — Add conditional metadata display in the file render block. Locate the template section that renders the list of uploaded file entries for a file-type question (where `file.name` or `file.originalFileName` is displayed). Inside each file entry element, after the filename span/text, add a conditional block:

    `<span v-if="file.uploadedBy || file.uploadDate" class="text-xs text-gray-500 ml-2">Uploaded by {{ file.uploadedBy }}, {{ timeZoneConvert(file.uploadDate, false, projectTenMinutesSsecondsDate) }}</span>`

    The `v-if` on `file.uploadedBy || file.uploadDate` ensures historical file answers (no metadata fields) render without error — the span is simply absent for those entries (per D-09).

    Do not change handleFileChange, transformFormDataForAPI, validateForm, or any other logic. Only touch the upload response handler and the file render block.
  </action>
  <verify>
    <automated>cd packages/flowFlex-common && pnpm type:check 2>&1 | grep -i "dynamicForm" || echo "no type errors in dynamicForm.vue"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "uploadedBy" packages/flowFlex-common/src/app/views/onboard/onboardingList/components/dynamicForm.vue` returns at least 2 matches: one in the upload response handler (injection), one in the template display block
    - `grep -n "uploadDate" packages/flowFlex-common/src/app/views/onboard/onboardingList/components/dynamicForm.vue` returns at least 2 matches: one in the injection, one in the template
    - `grep -n "timeZoneConvert" packages/flowFlex-common/src/app/views/onboard/onboardingList/components/dynamicForm.vue` returns a match in the file metadata display span
    - The display span has `v-if="file.uploadedBy || file.uploadDate"` (D-09 graceful degradation)
    - `pnpm type:check` reports no new errors in dynamicForm.vue
    - Behavior after upload: file entry shows "Uploaded by Jane Smith, 06/03/2026 14:20:47" to the right of the filename
    - Behavior for historical answers (no metadata): filename renders normally with no error and no blank metadata line
  </acceptance_criteria>
  <done>File upload response enriched with uploadedBy + uploadDate; metadata displayed conditionally beside filename; historical records unaffected</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /ow/questionnaires/v1/questions/upload-file | File binary crosses trust boundary; response includes server-assigned metadata |
| Upload response → answer JSONB | uploadedBy value originates from server-side _userContext.UserName — not user-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | uploadedBy field in JSONB | mitigate | Value is set server-side from `_userContext.UserName` (JWT-authenticated), not taken from client request body. Frontend only reads what the server returned — no client-controlled injection path. |
| T-03-02-02 | Information Disclosure | UploadedBy display name in UI | accept | Display name is already visible to case participants in other audit contexts (change log, assigned-to fields). No new disclosure surface. |
| T-03-02-03 | Tampering | timeZoneConvert rendering uploadDate | accept | uploadDate comes from server UploadTime (UTC); timeZoneConvert is a pure display function with no side effects. |
| T-03-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. |
</threat_model>

<verification>
After both tasks complete:

1. `cd packages/flowFlex-backend && dotnet build --no-restore 2>&1 | tail -5` — Build succeeded, 0 errors
2. `grep -n "UploadedBy" packages/flowFlex-backend/Application.Contracts/Dtos/OW/Questionnaire/QuestionnaireFileUploadResponseDto.cs` — returns a match
3. `grep -n "UploadedBy" packages/flowFlex-backend/WebApi/Controllers/OW/QuestionnaireController.cs` — returns a match
4. `cd packages/flowFlex-common && pnpm type:check 2>&1 | grep -i "dynamicForm" || echo "clean"` — no errors
5. `grep -c "uploadedBy" packages/flowFlex-common/src/app/views/onboard/onboardingList/components/dynamicForm.vue` — count >= 2
6. `grep -n "v-if=\"file.uploadedBy || file.uploadDate\"" packages/flowFlex-common/src/app/views/onboard/onboardingList/components/dynamicForm.vue` — returns a match (D-09 guard confirmed)
</verification>

<success_criteria>
- Uploading a file in a questionnaire results in the file entry displaying "Uploaded by {name}, MM/DD/YYYY HH:mm:ss" to the right of the filename
- The uploader name comes from the authenticated user's display name (server-assigned, not editable)
- Existing questionnaire answers with file attachments but no uploadedBy/uploadDate render correctly with just the filename — no blank line, no JS error
- dotnet build and pnpm type:check both pass with no new errors
</success_criteria>

<output>
Create `.planning/phases/03-frontend-ux-data/03-02-SUMMARY.md` when done
</output>
