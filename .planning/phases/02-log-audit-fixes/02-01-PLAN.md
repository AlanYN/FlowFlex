---
phase: 02-log-audit-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/flowFlex-backend/Application/Services/OW/ChangeLog/ChecklistLogService.cs
  - packages/flowFlex-backend/Application/Services/OW/OnboardingServices/OnboardingStageProgressService.cs
  - packages/flowFlex-common/src/app/views/onboard/onboardingList/components/dynamicForm.vue
autonomous: true
requirements:
  - LOG-01
  - LOG-02
  - LOG-03
  - UX-01

must_haves:
  truths:
    - "Completing a Checklist task writes operationDescription 'Completed the task' to the change log"
    - "Cancelling a Checklist task writes operationDescription 'Cancelled the task' to the change log"
    - "Saving a Stage does not produce any new entry in ff_operation_change_log"
    - "Clicking Next in the questionnaire form scrolls the full page to the top before rendering the next section"
  artifacts:
    - path: "packages/flowFlex-backend/Application/Services/OW/ChangeLog/ChecklistLogService.cs"
      provides: "BuildTaskCompletionDescription returns 'Completed the task'"
      contains: "Completed the task"
    - path: "packages/flowFlex-backend/Application/Services/OW/ChangeLog/ChecklistLogService.cs"
      provides: "BuildTaskUncompletionDescription returns 'Cancelled the task'"
      contains: "Cancelled the task"
    - path: "packages/flowFlex-backend/Application/Services/OW/OnboardingServices/OnboardingStageProgressService.cs"
      provides: "LogStageSaveAsync call removed and method body deleted"
    - path: "packages/flowFlex-common/src/app/views/onboard/onboardingList/components/dynamicForm.vue"
      provides: "scrollToTop helper called on all navigation paths in goToNextSection"
      contains: "window.scrollTo"
  key_links:
    - from: "ChecklistLogService.BuildTaskCompletionDescription"
      to: "ff_operation_change_log.operation_description"
      via: "_operationChangeLogService.LogOperationAsync"
      pattern: "Completed the task"
    - from: "dynamicForm.vue goToNextSection"
      to: "window"
      via: "scrollTo"
      pattern: "window\\.scrollTo.*top.*0"
---

<objective>
Fix three independent issues: (1) per D-06, change Checklist Done/Cancel log text in ChecklistLogService to "Completed the task" / "Cancelled the task"; (2) per D-07, remove the LogStageSaveAsync call site and dead method from OnboardingStageProgressService; (3) per D-03, add window.scrollTo({ top: 0, behavior: 'smooth' }) after nextTick in all navigation paths of dynamicForm.vue's goToNextSection.

Purpose: These are all text/call-site changes with no schema or API surface impact. All three are safe to ship together in Wave 1 because they touch disjoint files.
Output: Three modified files, zero new files.
</objective>

<execution_context>
@c:/Users/Kai.Li/.claude/get-shit-done/workflows/execute-plan.md
@c:/Users/Kai.Li/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-log-audit-fixes/02-CONTEXT.md
@.planning/phases/02-log-audit-fixes/02-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix Checklist log text (LOG-01 + LOG-02, per D-06)</name>
  <files>packages/flowFlex-backend/Application/Services/OW/ChangeLog/ChecklistLogService.cs</files>
  <read_first>
    packages/flowFlex-backend/Application/Services/OW/ChangeLog/ChecklistLogService.cs
    (Read the full BuildTaskCompletionDescription method at line ~413 and BuildTaskUncompletionDescription at line ~433 before editing.)
  </read_first>
  <action>
    In ChecklistLogService.cs, replace the body of BuildTaskCompletionDescription (private method, ~line 413) so it unconditionally returns the string "Completed the task". Remove the existing conditional branches that append completion notes and actual hours — per D-06, the description must be exactly this string with no other content.

    Replace the body of BuildTaskUncompletionDescription (private method, ~line 433) so it unconditionally returns the string "Cancelled the task". Remove the existing description variable and the commented-out reason block entirely.

    Do NOT change the method signatures, the OperationType enum values, the operationTitle strings, or any other logic in the file. Only the return values of these two private methods change.
  </action>
  <verify>
    <automated>cd packages/flowFlex-backend && dotnet build --no-restore -p:TreatWarningsAsErrors=false 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - BuildTaskCompletionDescription contains exactly: return "Completed the task";
    - BuildTaskUncompletionDescription contains exactly: return "Cancelled the task";
    - No other method signatures, enum values, or operationTitle strings are modified
    - dotnet build exits with code 0
  </acceptance_criteria>
  <done>Both description methods return the exact JIRA-specified strings; build passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove LogStageSaveAsync (LOG-03, per D-07)</name>
  <files>packages/flowFlex-backend/Application/Services/OW/OnboardingServices/OnboardingStageProgressService.cs</files>
  <read_first>
    packages/flowFlex-backend/Application/Services/OW/OnboardingServices/OnboardingStageProgressService.cs
    (Read lines 1275–1360 to confirm the exact call site at line 1284, the if-block boundary, and the full private method body from ~line 1302 to 1357 before editing.)
  </read_first>
  <action>
    In OnboardingStageProgressService.cs, remove the entire if-block at lines 1282–1285:
      if (result)
      {
          await LogStageSaveAsync(onboarding, stageId, stageProgress);
      }

    Then remove the entire LogStageSaveAsync private method (lines ~1299–1357 including its XML summary comment). The surrounding structure must remain intact: the `var result = await SafeUpdateOnboardingAsync(...)` line and `return result;` line must stay unchanged.

    After removing the method body, search for all other usages of `_stageRepository` in this same file. If `_stageRepository` has no remaining usages after the removal, also remove its private field declaration and constructor injection parameter. Do NOT remove OperationTypeEnum.StageSave — it stays in the enum.
  </action>
  <verify>
    <automated>cd packages/flowFlex-backend && dotnet build --no-restore -p:TreatWarningsAsErrors=false 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The string "LogStageSaveAsync" does not appear anywhere in OnboardingStageProgressService.cs
    - The string "await SafeUpdateOnboardingAsync" is still present at the same location
    - The string "return result;" immediately follows the removed block
    - If _stageRepository had no other call sites, its field declaration is also absent from the file
    - OperationTypeEnum.StageSave still exists in the enum file (do not modify it)
    - dotnet build exits with code 0
  </acceptance_criteria>
  <done>No StageSave log call remains; dead method and any orphaned field are cleaned up; build passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Scroll to top on questionnaire navigation (UX-01, per D-03)</name>
  <files>packages/flowFlex-common/src/app/views/onboard/onboardingList/components/dynamicForm.vue</files>
  <read_first>
    packages/flowFlex-common/src/app/views/onboard/onboardingList/components/dynamicForm.vue
    (Read lines 1549–1600 to see goToPreviousSection, goToNextSection, and goToSection in full before editing.)
  </read_first>
  <action>
    In dynamicForm.vue, add a private helper function scrollToTop immediately before goToPreviousSection (around line 1552):

      const scrollToTop = () => {
          nextTick(() => {
              window.scrollTo({ top: 0, behavior: 'smooth' });
          });
      };

    nextTick is already imported at the top of the script block (confirmed at line 670).

    In goToNextSection: add a scrollToTop() call on both navigation paths:
    - After the early-return path: call scrollToTop() immediately before the `return;` statement inside the if (targetSectionIndex !== -1) block
    - After the default path: call scrollToTop() after the if (!isLastSection.value) block, before the closing brace of goToNextSection

    Also add scrollToTop() at the end of goToPreviousSection (after the currentSectionIndex.value-- line) and at the end of goToSection (after the currentSectionIndex.value = index line) for UX consistency.

    Do not modify any other logic, template markup, or existing imports.
  </action>
  <verify>
    <automated>cd packages/flowFlex-common && pnpm type:check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - scrollToTop function is defined and contains: window.scrollTo({ top: 0, behavior: 'smooth' })
    - goToNextSection calls scrollToTop() on BOTH the jump-rule path (before return) and the default path (after currentSectionIndex increment)
    - goToPreviousSection calls scrollToTop() after decrementing currentSectionIndex
    - goToSection calls scrollToTop() after setting currentSectionIndex
    - nextTick is NOT re-imported (already present at line 670)
    - pnpm type:check exits with code 0
  </acceptance_criteria>
  <done>All navigation paths in the questionnaire form trigger a smooth scroll to the top of the page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Log text is server-generated; client cannot influence operationDescription content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | ChecklistLogService description strings | accept | Hardcoded return values; no user input reaches these methods |
| T-02-02 | Information Disclosure | LogStageSaveAsync removal | accept | Removing log emission reduces data exposure, not increases it |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After all three tasks complete:
1. `cd packages/flowFlex-backend && dotnet build` exits 0
2. `cd packages/flowFlex-common && pnpm type:check` exits 0
3. Grep confirms: `grep -n "Completed the task" packages/flowFlex-backend/Application/Services/OW/ChangeLog/ChecklistLogService.cs` returns a match
4. Grep confirms: `grep -n "Cancelled the task" packages/flowFlex-backend/Application/Services/OW/ChangeLog/ChecklistLogService.cs` returns a match
5. Grep confirms: `grep -c "LogStageSaveAsync" packages/flowFlex-backend/Application/Services/OW/OnboardingServices/OnboardingStageProgressService.cs` returns 0
6. Grep confirms: `grep -n "window.scrollTo" packages/flowFlex-common/src/app/views/onboard/onboardingList/components/dynamicForm.vue` returns a match
</verification>

<success_criteria>
- LOG-01: BuildTaskCompletionDescription returns exactly "Completed the task"
- LOG-02: BuildTaskUncompletionDescription returns exactly "Cancelled the task"
- LOG-03: LogStageSaveAsync call site and method body are absent from OnboardingStageProgressService.cs; build clean
- UX-01: goToNextSection calls window.scrollTo({ top: 0, behavior: 'smooth' }) on all navigation paths; type-check clean
</success_criteria>

<output>
Create `.planning/phases/02-log-audit-fixes/02-01-SUMMARY.md` when done
</output>
