---
phase: 02-log-audit-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - packages/flowFlex-backend/Domain/Repository/OW/IChecklistTaskNoteRepository.cs
  - packages/flowFlex-backend/SqlSugarDB/Repositories/OW/ChecklistTaskNoteRepository.cs
  - packages/flowFlex-backend/Application/Services/OW/ChecklistTaskService.cs
  - packages/flowFlex-backend/Application/Services/OW/WorkflowService.cs
autonomous: false
requirements:
  - LOG-04
  - DATA-03

must_haves:
  truths:
    - "The comment count badge on a Checklist task matches the count of Notes-type entries only, excluding soft-deleted notes"
    - "A workflow deactivated via the management page shows as Inactive in both the management list and the Case creation dropdown"
  artifacts:
    - path: "packages/flowFlex-backend/Domain/Repository/OW/IChecklistTaskNoteRepository.cs"
      provides: "CountByTaskIdAsync accepts optional noteType filter parameter"
      contains: "CountByTaskIdAsync"
    - path: "packages/flowFlex-backend/SqlSugarDB/Repositories/OW/ChecklistTaskNoteRepository.cs"
      provides: "CountByTaskIdAsync filters by noteType when provided"
    - path: "packages/flowFlex-backend/Application/Services/OW/ChecklistTaskService.cs"
      provides: "GetNotesCountByTaskIdsAsync passes confirmed NoteType string to CountByTaskIdAsync"
    - path: "packages/flowFlex-backend/Application/Services/OW/WorkflowService.cs"
      provides: "UpdateAsync syncs IsActive from Status after AutoMapper mapping"
  key_links:
    - from: "ChecklistTaskService.GetNotesCountByTaskIdsAsync"
      to: "ChecklistTaskNoteRepository.CountByTaskIdAsync"
      via: "_noteRepository.CountByTaskIdAsync(taskId, confirmedNoteType)"
      pattern: "CountByTaskIdAsync.*taskId"
    - from: "WorkflowService.UpdateAsync"
      to: "entity.IsActive"
      via: "entity.IsActive = entity.Status?.ToLower() == \"active\""
      pattern: "IsActive.*Status"
---

<objective>
Fix two independent issues that each require a DB investigation step before code changes can be made safely.

LOG-04 (per D-04, D-05): Query staging DB to confirm the exact NoteType string used for user-visible notes, then add an optional noteType filter to CountByTaskIdAsync so GetNotesCountByTaskIdsAsync only counts Notes entries (not deleted ones, not change-log entries).

DATA-03 (per D-08): Query staging DB to confirm whether ff_workflow rows exist with status='inactive' but is_active=true (the suspected sync gap), then fix WorkflowService.UpdateAsync to derive entity.IsActive from entity.Status after AutoMapper mapping, ensuring deactivate always produces a consistent DB state.

Purpose: Both fixes close data accuracy gaps visible to end users. Both require DB state verification before hardcoding values.
Output: Four modified backend files; no frontend changes; no schema migrations.
</objective>

<execution_context>
@c:/Users/Kai.Li/.claude/get-shit-done/workflows/execute-plan.md
@c:/Users/Kai.Li/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-log-audit-fixes/02-CONTEXT.md
@.planning/phases/02-log-audit-fixes/02-RESEARCH.md
@.planning/phases/02-log-audit-fixes/02-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: DB investigation — confirm NoteType discriminator and Workflow IsActive sync gap (per D-04, D-08)</name>
  <what-built>
    Two SQL queries need to be run against the staging database (wfe-flowflex-postgres-staging) before the code changes can be made. The executor cannot hardcode the NoteType string without DB confirmation (D-04), and the DATA-03 fix strategy depends on whether the IsActive/Status sync gap actually exists in DB data.
  </what-built>
  <how-to-verify>
    Run both queries in the wfe-flowflex-postgres-staging MCP tool and report the results here.

    Query 1 — NoteType discriminator (for LOG-04):
      SELECT DISTINCT note_type, COUNT(*) as cnt
      FROM ff_checklist_task_note
      WHERE is_valid = true
      GROUP BY note_type
      ORDER BY cnt DESC;

    Expected: reveals the exact string(s) used for user-created notes. Likely values: "General", "Notes", or similar. Record the exact string that corresponds to notes visible in the Notes tab UI.

    Also run this to check the is_deleted distribution:
      SELECT is_deleted, COUNT(*) as cnt
      FROM ff_checklist_task_note
      WHERE is_valid = true
      GROUP BY is_deleted;

    Query 2 — Workflow IsActive/Status sync gap (for DATA-03):
      SELECT id, name, status, is_active, modify_date
      FROM ff_workflow
      WHERE is_valid = true
      ORDER BY modify_date DESC
      LIMIT 20;

    Expected: if rows exist where status = 'inactive' AND is_active = true (or status = 'active' AND is_active = false), the sync gap is confirmed. If all rows are consistent, DATA-03 may be a display issue instead.

    Report back:
    - Exact NoteType string to use in the code filter
    - Whether the IsActive/Status sync gap exists in DB
  </how-to-verify>
  <resume-signal>Paste the query results here, then type "approved" to continue to the code changes</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Filter Notes count by NoteType (LOG-04, per D-04, D-05)</name>
  <files>
    packages/flowFlex-backend/Domain/Repository/OW/IChecklistTaskNoteRepository.cs
    packages/flowFlex-backend/SqlSugarDB/Repositories/OW/ChecklistTaskNoteRepository.cs
    packages/flowFlex-backend/Application/Services/OW/ChecklistTaskService.cs
  </files>
  <read_first>
    packages/flowFlex-backend/Domain/Repository/OW/IChecklistTaskNoteRepository.cs
    (Read the CountByTaskIdAsync interface signature at line 53.)
    packages/flowFlex-backend/SqlSugarDB/Repositories/OW/ChecklistTaskNoteRepository.cs
    (Read CountByTaskIdAsync implementation at lines 124–130.)
    packages/flowFlex-backend/Application/Services/OW/ChecklistTaskService.cs
    (Read GetNotesCountByTaskIdsAsync at lines 1063–1074.)
  </read_first>
  <action>
    Use the exact NoteType string confirmed from the DB investigation in Task 1. Substitute that confirmed string wherever the placeholder [CONFIRMED_NOTE_TYPE] appears below.

    Step 1 — Update the interface in IChecklistTaskNoteRepository.cs:
    Change the signature from:
      Task&lt;int&gt; CountByTaskIdAsync(long taskId);
    To:
      Task&lt;int&gt; CountByTaskIdAsync(long taskId, string noteType = null);

    Step 2 — Update the implementation in ChecklistTaskNoteRepository.cs:
    Change CountByTaskIdAsync to accept the optional noteType parameter. When noteType is not null or empty, add a .Where condition: ctn.NoteType == noteType. The existing !ctn.IsDeleted and ob.IsValid filters must remain. Per D-05, IsDeleted is already explicitly filtered here so the count is already excluding deleted notes — verify the existing filter is still present and do not remove it.

    The updated query chain must be:
      base.db.Queryable&lt;ChecklistTaskNote&gt;()
        .LeftJoin&lt;Onboarding&gt;((ctn, ob) => ctn.OnboardingId == ob.Id)
        .Where((ctn, ob) => ctn.TaskId == taskId &amp;&amp; !ctn.IsDeleted &amp;&amp; ob.IsValid)
        .WhereIF(!string.IsNullOrEmpty(noteType), (ctn, ob) => ctn.NoteType == noteType)
        .CountAsync();

    Use SqlSugar's WhereIF extension for the conditional filter — it is already used elsewhere in the codebase.

    Step 3 — Update the service call in ChecklistTaskService.cs:
    In GetNotesCountByTaskIdsAsync at line 1069, change:
      var count = await _noteRepository.CountByTaskIdAsync(taskId);
    To:
      var count = await _noteRepository.CountByTaskIdAsync(taskId, "[CONFIRMED_NOTE_TYPE]");

    Substitute [CONFIRMED_NOTE_TYPE] with the exact string from the DB query result.
  </action>
  <verify>
    <automated>cd packages/flowFlex-backend && dotnet build --no-restore -p:TreatWarningsAsErrors=false 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - IChecklistTaskNoteRepository.CountByTaskIdAsync signature has noteType parameter with default null
    - ChecklistTaskNoteRepository.CountByTaskIdAsync uses WhereIF to conditionally filter by noteType
    - The existing !ctn.IsDeleted filter is still present in the WHERE clause (per D-05)
    - GetNotesCountByTaskIdsAsync in ChecklistTaskService.cs passes the confirmed NoteType string literal
    - dotnet build exits with code 0
    - No other callers of CountByTaskIdAsync are broken (default null preserves existing behavior for any other call sites)
  </acceptance_criteria>
  <done>Notes count query filters by confirmed NoteType string; deleted notes remain excluded; build passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Sync IsActive from Status in WorkflowService.UpdateAsync (DATA-03, per D-08)</name>
  <files>packages/flowFlex-backend/Application/Services/OW/WorkflowService.cs</files>
  <read_first>
    packages/flowFlex-backend/Application/Services/OW/WorkflowService.cs
    (Read UpdateAsync starting at line 163. Focus on lines 236–246 where _mapper.Map(input, entity) is called and entity is then saved. The fix must be inserted between the mapper call and the UpdateAsync repository call.)
  </read_first>
  <action>
    In WorkflowService.UpdateAsync, after the line `_mapper.Map(input, entity);` (line ~237) and after the `entity.IsAIGenerated = originalIsAIGenerated;` guard (line ~241), add a single line that derives IsActive from the Status field:

      entity.IsActive = string.Equals(entity.Status, "active", StringComparison.OrdinalIgnoreCase);

    This ensures that whenever UpdateAsync is called — including the frontend deactivate path that sends status='inactive' but may not send isActive=false explicitly — the IsActive bool is always consistent with the Status string. This is the correct single-source-of-truth fix (backend sync, per D-08).

    Do not modify ActivateAsync or DeactivateAsync — those already set both fields explicitly. The fix is only in UpdateAsync.

    If the DB investigation in Task 1 revealed the sync gap does NOT exist (all rows are consistent), still apply this fix — it is a defensive correctness improvement that prevents future inconsistency.
  </action>
  <verify>
    <automated>cd packages/flowFlex-backend && dotnet build --no-restore -p:TreatWarningsAsErrors=false 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - WorkflowService.UpdateAsync contains: entity.IsActive = string.Equals(entity.Status, "active", StringComparison.OrdinalIgnoreCase);
    - The line appears after _mapper.Map(input, entity) and before _workflowRepository.UpdateAsync(entity)
    - ActivateAsync and DeactivateAsync are NOT modified
    - dotnet build exits with code 0
  </acceptance_criteria>
  <done>WorkflowService.UpdateAsync always writes a consistent IsActive value derived from Status; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | WorkflowInputDto.Status is user-supplied; IsActive must be derived server-side, not trusted from client |
| DB → Service | NoteType is stored data; filter string must be confirmed from DB before hardcoding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | WorkflowService.UpdateAsync IsActive field | mitigate | Derive IsActive server-side from Status string; ignore any IsActive value sent by client via UpdateAsync path |
| T-02-04 | Information Disclosure | ff_checklist_task_note NoteType filter | accept | Filter only restricts count aggregation; no sensitive data exposed |
| T-02-05 | Elevation of Privilege | Workflow status manipulation | mitigate | IsActive derived from Status; client cannot set IsActive=true while Status='inactive' via UpdateAsync |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After all tasks complete:
1. `cd packages/flowFlex-backend && dotnet build` exits 0
2. Grep confirms NoteType filter: `grep -n "WhereIF" packages/flowFlex-backend/SqlSugarDB/Repositories/OW/ChecklistTaskNoteRepository.cs` returns a match
3. Grep confirms service passes noteType arg: `grep -n "CountByTaskIdAsync" packages/flowFlex-backend/Application/Services/OW/ChecklistTaskService.cs` shows two arguments in the call
4. Grep confirms IsActive sync: `grep -n "entity.IsActive.*entity.Status" packages/flowFlex-backend/Application/Services/OW/WorkflowService.cs` returns a match
5. Manual browser smoke test: deactivate a workflow in the management page, then open Case creation dropdown — the same workflow must show the Inactive tag
</verification>

<success_criteria>
- LOG-04: Comment count badge on a Checklist task reflects only Notes-type entries that are not soft-deleted; count matches what the Notes tab shows
- DATA-03: After deactivating a workflow, the Case creation dropdown and the management page both show it as Inactive, driven by a consistent is_active value in the DB
</success_criteria>

<output>
Create `.planning/phases/02-log-audit-fixes/02-02-SUMMARY.md` when done
</output>
