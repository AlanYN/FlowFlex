# OW-695 方案讨论

## 背景

UT Customer Onboarding 流程中：

- 客户在 **Stage 2** 填写公司信息（Credit Application）
- Stage 2 完成后，**Accounting** 在 **Stage 3** 进行 Credit Evaluation
- Accounting 发现客户填错时，无法修改 Stage 2 的答案（已 Completed 锁定）
- 如果带错误数据传给 CRM，后续还需要去 CRM 手动修正

BA 提出的 workaround：将 Stage 2 的问卷复制一份放到 Stage 3，Stage 2 完成时自动同步答案，Accounting 在 Stage 3 修改。

---

## 为什么不能用"复刻问卷到 Stage 3"的方案

### 系统取值逻辑

系统里有一个 Integration Action 的功能，用于在 Stage 完成时自动调用外部接口（比如把客户信息推给 CRM 创建 Customer Account）。调用时，传参是这样取问卷里的答案的：

```
{{questionnaireAnswerByQuestionId.问题ID}}
```

**系统的取值逻辑是这样的：** 拿着这个"问题 ID"，去整个 Case 的所有 Stage 里扫一遍，找到这道题的答案，然后传给 CRM。

这个逻辑本来没问题，因为正常情况下每道题在整个 Workflow 里只出现一次。

---

### 复刻方案和现有逻辑的冲突

我们查了系统里问卷的数据结构，发现**题目的 ID 是问卷本身的一部分，复制问卷时会原封不动地带过去，不会重新生成**。

这意味着：把 Stage 2 的问卷复制一份放到 Stage 3，**两份问卷里每道题的 ID 完全一样**——"公司名称"这道题，在 Stage 2 和 Stage 3 副本里是同一个 ID。

这就和系统现有取值逻辑直接冲突了：

当 Integration Action 调用 CRM 接口，用 `{{questionnaireAnswerByQuestionId.问题ID}}` 取"公司名称"时，系统会同时找到两条记录——Stage 2 客户填的原始答案，和 Stage 3 Accounting 修正后的答案。**两条记录的 ID 一样，系统不知道该取哪个，结果取决于数据库返回的顺序，完全随机。**

我们看了取答案的源码，确认里面没有任何优先级判断逻辑，不会自动选"最新的"或"Stage 3 的"。系统不会报错，数据会悄悄传错给 CRM，等发现的时候已经晚了。

**简单说：问卷复制后题目 ID 不变，同一个 ID 在两个 Stage 都有答案，系统取值会乱，这是和现有取值逻辑的根本冲突，无法通过配置绕开。**

---

## 我们建议的两个替代方案

### 方案一：重新打开 Stage 2（Reopen）

**思路：** 当 Accounting 发现 Stage 2 有填错的内容时，直接把 Stage 2 重新打开，让它回到可编辑状态，客户或内部人员修正后重新 Complete。

**怎么实现：** 在 Workflow 的 Stage 配置里，指定哪些团队有权限"Reopen"这个 Stage。有权限的人点一个按钮，Stage 2 就解锁了，可以重新编辑。

**这个不难做：** 只需要把 Stage 2 的完成状态重置，不涉及整个流程的回退，比 BA 最初提到的"Rollback"简单很多。

**需要 BA 确认的点：**

1. Reopen 这个操作由谁来触发？Accounting 自己，还是需要管理员？
2. Stage 2 被重新打开后，Stage 3 的工作怎么处理？建议 Stage 3 保持不动，但自动通知 Stage 3 的负责人"Stage 2 有更新，请留意"，主动权留给 Accounting 自己判断。
3. Stage 2 重新 Complete 后，之前配置的 Action（比如推数据给 CRM）是否需要重新触发？

---

### 方案二：给特定团队开放 Stage 2 的答案编辑权限（Amend）

**思路：** Stage 2 的状态保持 Completed 不变，但给 Accounting 团队开一个"批改"权限，允许直接在 Stage 2 里修正问卷答案，每次修改都有记录，可以看到原始答案和改了什么。

**怎么实现：** 同样在 Workflow 的 Stage 配置里设置，指定哪些团队有 Amend 权限。有权限的用户进入 Stage 2，看到"Amend"按钮，点击后可以编辑，保存后原始答案保留、新答案生效。

**优势：** Stage 流程不受影响，Accounting 随时可以修正，数据只有一份，CRM 取值不受任何影响。

---

## 两个方案对比

| 对比项       | BA 方案（复刻问卷） | 方案一（Reopen Stage）         | 方案二（Amend 权限） |
| ------------ | ------------------- | ------------------------------ | -------------------- |
| 数据份数     | 两份，容易混        | 一份                           | 一份                 |
| CRM 取值风险 | 有，可能静默出错    | 无                             | 无                   |
| 流程影响     | 无                  | Stage 2 重新打开               | 无                   |
| 通知机制     | 无                  | 需要通知 Stage 3               | 不需要               |
| 开发难度     | 中（但有系统冲突）  | 低                             | 低                   |
| 需要 BA 决策 | —                   | Reopen 权限 + Stage 3 处理方式 | Amend 权限归属       |

---

## 待 BA 确认

请 BA 根据业务实际情况选择方案一或方案二，并回答对应的问题，我们按确认后的方案推进开发。
